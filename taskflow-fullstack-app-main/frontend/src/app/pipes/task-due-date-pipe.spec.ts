import { TaskDueDatePipe } from './task-due-date-pipe';

describe('TaskDueDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskDueDatePipe();
    expect(pipe).toBeTruthy();
  });
});
